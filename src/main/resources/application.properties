# --- [Application Info] ---
spring.application.name=BitWay_Back

# --- [DataSource (PostgreSQL)] ---
spring.datasource.url=jdbc:postgresql://localhost:5432/bitway
spring.datasource.username=bitway_user
spring.datasource.password=bitway_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# --- [JPA ??] --- pring.jpa.hibernate.ddl-auto=update  = Entity? ??? ?? ??? ??.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- [?? ?? ??] ---?
server.port=8080
server.address=0.0.0.0

# --- [CORS ?? ??] ---
spring.mvc.cors.allowed-origins=*

# --- [?? ??] ---
logging.level.root=INFO
